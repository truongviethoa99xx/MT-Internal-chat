# PostgreSQL — nguồn chân lý
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mtm_chat?schema=public"

# Redis — Socket.IO adapter (scale nhiều instance), presence, rate-limit. KHÔNG dùng cấp seq.
REDIS_URL="redis://localhost:6379"

# CORS cho web client
WEB_ORIGIN="http://localhost:5173"

PORT=3000
